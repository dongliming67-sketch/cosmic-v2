# ===================================================
# 开放平台模式配置说明
# ===================================================
#
# 本系统已配置为"开放平台模式"：
# - 每个用户在网页端输入自己的 SiliconFlow API Key
# - 服务端不需要预设任何 API 密钥
# - 用户的 Key 保存在用户自己的浏览器 localStorage 中
# - 每次 API 请求都会携带用户自己的 Key
#
# 部署到 Render 时，只需设置以下环境变量：
# - PORT=3001 (或不设置，使用默认值)
#
# ===================================================

# 服务器端口（部署到 Render 时，Render 会自动设置此变量）
PORT=3001

# ===================================================
# 以下配置在开放平台模式下不需要设置
# 仅供本地开发或私有部署参考
# ===================================================

# SiliconFlow/DeepSeek 配置（如果想在服务端预设密钥，取消注释以下行）
# OPENAI_API_KEY=your_siliconflow_api_key
# OPENAI_BASE_URL=https://api.siliconflow.cn/v1
# OPENAI_MODEL=deepseek-ai/DeepSeek-R1-Distill-Qwen-32B
# THREE_LAYER_PROVIDER=openai
